#include<stdio.h>
#include<string.h>
#define ROW 100
#define	SIZE 250

int main(){
		FILE *dosya1,*dosya2,*dosya3;
		char matris[ROW][SIZE],list[10][10],ch1,ch2;
		char (*ptr)[10];	
		int i=0,j=0,k=0,t,boyut,count,temp,status;
		
		ptr=list;
		
		
		
		dosya1=fopen("input1.txt","r");
			i=0;
			 status = fscanf(dosya1,"%s",(*ptr+i));
            while( status!= EOF && status!=0){     
                 printf("%s\n",(*ptr+i));                         //sonradan silinecek
				  i++;            
				  status = fscanf(dosya1,"%s",(*ptr+i));             
		}
		
		
		
		
		dosya2=fopen("input2.txt","r");
		count=0;
			while(fscanf(dosya2, "%s", matris[count]) != EOF){
						
					printf("%s",matris[count]);
					count++;
					
					}
				printf("\n");
				
				
		
		for(i=0; i < count; i++){	/*string içerisinde tarama yapmak için satýr sayýsý kadar döngü yaptým */

		for(j=0; matris[i][j] != '\0'; j++){ 	/her satýrda null karakterine kadar tarama yaptým/
			
			temp = 0; 
			
			if(matris[i][j] == list[0]){	/eðer karakter kelimenin ilk harfi ile eþleþirse koþul saðlanýr/
				temp++;
				
				for(k=1; k < strlen(list); k++){		/kelimeyi saða doðru kelime uzunluðu kadar karakterle karþýlaþtýrdým/
					if(matris[i][j+k] == list[k])
						temp++;
				}
				
				if(temp == strlen(list))	/iþlem sonunda tempteki sayý kelime uzunluðuna eþitse kelime bulunmuþtur/
					printf("%s (%d,%d) Horizontal",list, i+1, j+1); /kelimeyi yazdýrdým/
					
				else	temp = 1;	/kelime yatay olarak bulunamadýysa dikey olarak bakýlmasý için alttaki ifin koþulunu saðladým/
				
				
				if(temp == 1){

					for(k=1; k < strlen(list); k++){	/kelimeyi karakterin aþaðýsýna doðru dikey olarak kelime uzunluðu kadar karakterle karþýlaþtýrdým/
						if(matris[i+k][j] == list[k])
							temp++;
					}
					if(temp == strlen(list))	/iþlem sonunda tempteki sayý kelime uzunluðuna eþitse kelime bulunmuþtur/
						printf("%s (%d,%d) Vertical",list, i+1, j+1);	/kelimeyi yazdýrdým/
}
}
}
}

	
		//dosya3=fopen("output.txt","w");
	
	
		fclose(dosya1);
		fclose(dosya2);
		//fclose(dosya3);
	
	return 0;
		
}